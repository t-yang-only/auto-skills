# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
build/
dist/
*.egg-info/

# Environments
venv/
.venv/
env/

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp

# Private Evolution Vault (双轨隔离区：存放用户私人习惯、私有凭据、自孵化技能与Obsidian同步数据)
# 确保上游公开库拉取更新 (git pull) 时绝对不覆盖、不冲突、不泄露用户的私人进化内容！
.evolution/
# 分发快照（六根链接共用；含指回 .evolution 的链接，绝不能提交）
.dist/

*.private.*
.sendkey


config/gateway.token

# 数据库凭据（绝不提交：本仓库有公开远端）
config/db.password
config/gateway.url

# nm-skills 运行时台账（各 Agent 的认领/工作日志，属本地协作状态，不进版本库）
agent_word/
