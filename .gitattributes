# 统一行尾：仓库内一律 LF。
# Windows 上 core.autocrlf=true 会把检出改成 CRLF，导致：
#   - Python 里 "```text\n" 这类锚点在 CRLF 文件上匹配失败
#   - shell 脚本出现 bad interpreter
* text=auto eol=lf

# 二进制不做行尾转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.zip binary
*.gz binary
*.bundle binary
*.pdf binary
