# 把数据库密码写在这里（读取时两端空白会被 strip）。
# 该文件必须保持在 .gitignore 里，绝不要提交。
#
# 位置：config/db.password
# 对应：config/config.yaml -> database.password_file: config/db.password
#
# 改用环境变量：database.password_env: AUTO_SKILLS_DB_PASSWORD
# 改用 SQLite  ：database.type: sqlite（无需任何密码）
<在此粘贴密码>